{
  "name": "Module-8",
  "version": "1.0.0",
  "scripts": {
    "test": "npm run test:api",
    "test:api": "jest tests/booking.test.js --runInBand",
    "test:ui": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.42.0",
    "jest": "^29.7.0",
    "supertest": "^7.1.0"
  }
}
